{
  "name": "mtd",
  "version": "1.0.0",
  "description": "Missing Translation Detector — find untranslated keys in deployed web pages",
  "type": "module",
  "bin": {
    "mtd": "./scan.mjs"
  },
  "scripts": {
    "scan": "node scan.mjs",
    "build:extension": "cp bookmarklet.js extension/content.js && cd extension && zip -r ../mtd-extension.zip . && rm content.js"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
